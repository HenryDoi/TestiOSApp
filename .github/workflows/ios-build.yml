name: Build iOS App

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and Test iOS App
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable

    - name: Show Xcode version
      run: xcodebuild -version

    - name: Clean build folder
      run: |
        xcodebuild clean -project TestiOSApp.xcodeproj -scheme TestiOSApp

    - name: Build iOS App
      run: |
        xcodebuild build -project TestiOSApp.xcodeproj -scheme TestiOSApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest'

    - name: Archive iOS App
      run: |
        xcodebuild archive \
          -project TestiOSApp.xcodeproj \
          -scheme TestiOSApp \
          -configuration Release \
          -archivePath build/TestiOSApp.xcarchive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO

    - name: Export IPA
      run: |
        mkdir -p build
        cat > build/export_options.plist << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
          <key>method</key>
          <string>ad-hoc</string>
          <key>signingStyle</key>
          <string>automatic</string>
          <key>stripSwiftSymbols</key>
          <true/>
          <key>teamID</key>
          <string></string>
        </dict>
        </plist>
        EOF
        
        # Create a proper IPA structure manually
        echo "Creating IPA manually..."
        mkdir -p build/Payload
        cp -r build/TestiOSApp.xcarchive/Products/Applications/TestiOSApp.app build/Payload/
        cd build
        zip -r TestiOSApp_manual.ipa Payload/
        cd ..

    - name: Upload IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TestiOSApp-IPA
        path: |
          build/*.ipa
          build/*_unsigned.ipa
        retention-days: 30

    - name: List build outputs
      run: |
        echo "Build artifacts:"
        find build -name "*.ipa" -o -name "*.xcarchive" | head -10